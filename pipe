pipeline {
    agent any

    stages {
        stage('checkout') {
            steps {
                git branch: 'main', url: 'https://github.com/lekhraj99/lekhraj.git'
            }
        }
		stage('install docker') {
            steps {
                sh'''
				sudo apt-get install -y docker
				sudo systemctl start docker 
				sudo systemctl enable docker 
				'''
            }
        }
		
            }
        }
		
    }
}
