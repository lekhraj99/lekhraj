pipeline {
    agent any

    stages {
        stage('checkout') {
            steps {
                git 'https://github.com/lekhraj99/lekhraj.git'
            }
        }
		stage('install docker') {
            steps {
                sh'''
				sudo apt-get install -y docker
				sudo systemctl start docker 
				sudo systemctl enable docker 
				'''
            }
        }
		
            }
        }
		
    }
}
